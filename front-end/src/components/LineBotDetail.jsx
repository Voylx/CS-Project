import React from "react";
import { Button } from "react-bootstrap";

export const LineBotDetail = () => {
  return <div>LineBotDetail</div>;
  <Button
    onClick={() => {
      navigate(`../${botData.Bot_id}`);
    }}
  >
    Control
  </Button>;
};
